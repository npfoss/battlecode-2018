import bc.*;

/*
for squads, so they know what to do with themselves
*/
public enum Objective {
    NONE(),
    EXPLORE(),
    MINE(),
    BUILD(),
    BOARD_ROCKET(),
    DEFEND_LOC(),
    ATTACK_LOC();
}
